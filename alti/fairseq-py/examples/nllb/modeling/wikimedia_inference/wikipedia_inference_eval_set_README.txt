A blank line separates sections of different articles

The first line in each section is the title if that section had a title

The fulltext file is the text of the entire paragraph.

The sentence split file is the paragraph split into sentences

The markup split file is the paragraph split as if it was returned by markdown